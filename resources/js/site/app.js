// QCP marketing site scripts (Phase 08b-i). Bootstrap's JS bundle, GSAP, and
// jQuery load via CDN in the website layout (matching the legacy site). Custom
// component behavior (navigation, footer, scroll animation) is ported here in
// Increment 1.
